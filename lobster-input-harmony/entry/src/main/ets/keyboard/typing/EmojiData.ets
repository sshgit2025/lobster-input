// 对齐 Android: keyboard/typing/EmojiData.kt(数据表全量搬运,一个字符不少)

/** emoji 分类。 */
export class EmojiCategory {
  readonly title: string;
  readonly items: string[];

  constructor(title: string, items: string[]) {
    this.title = title;
    this.items = items;
  }
}

/**
 * emoji 数据(内置,系统字体渲染,不打包图片资源)。
 * 分类 + 常用映射(供 emoji 联想:词 → emoji)。
 */
export class EmojiData {

  static readonly categories: EmojiCategory[] = [
    new EmojiCategory('😀', [
      '😀', '😁', '😂', '🤣', '😃', '😄', '😅', '😆', '😉', '😊', '😋', '😎', '😍', '😘', '🥰', '😗', '😙', '😚',
      '🙂', '🤗', '🤩', '🤔', '🤨', '😐', '😑', '😶', '🙄', '😏', '😣', '😥', '😮', '🤐', '😯', '😪', '😫', '🥱',
      '😴', '😌', '😛', '😜', '😝', '🤤', '😒', '😓', '😔', '😕', '🙃', '🤑', '😲', '🙁', '😖', '😞', '😟', '😤',
      '😢', '😭', '😦', '😧', '😨', '😩', '🤯', '😬', '😰', '😱', '🥵', '🥶', '😳', '🤪', '😵', '🥴', '😠', '😡',
      '🤬', '😷', '🤒', '🤕', '🤢', '🤮', '🥳', '😇', '🤠', '🥺', '🤥', '🤫', '🤭', '🧐', '🤓'
    ]),
    new EmojiCategory('👍', [
      '👍', '👎', '👌', '✌️', '🤞', '🤟', '🤘', '👏', '🙌', '👐', '🤲', '🙏', '🤝', '💪', '👊', '✊', '🤛', '🤜',
      '👋', '🖐️', '✋', '🖖', '👆', '👇', '👈', '👉', '☝️', '✍️', '💅', '🤳', '💃', '🕺', '👀', '👁️', '👅', '👄'
    ]),
    new EmojiCategory('❤️', [
      '❤️', '🧡', '💛', '💚', '💙', '💜', '🖤', '🤍', '🤎', '💔', '❣️', '💕', '💞', '💓', '💗', '💖', '💘', '💝',
      '💯', '💢', '💬', '💭', '💤', '✨', '⭐', '🌟', '💫', '🔥', '🎉', '🎊', '🎁', '🌈', '☀️', '🌙', '⚡', '❄️'
    ]),
    new EmojiCategory('🐶', [
      '🐶', '🐱', '🐭', '🐹', '🐰', '🦊', '🐻', '🐼', '🐨', '🐯', '🦁', '🐮', '🐷', '🐸', '🐵', '🐔', '🐧', '🐦',
      '🦄', '🐝', '🦋', '🐢', '🐠', '🐳', '🌸', '🌹', '🌺', '🌻', '🌴', '🌵', '🍀', '🍎', '🍓', '🍑', '🍉', '🍇'
    ]),
    new EmojiCategory('⚽', [
      '⚽', '🏀', '🏈', '⚾', '🎾', '🏐', '🎱', '🏓', '🏸', '🥅', '🎮', '🎲', '🎯', '🎸', '🎤', '🎧', '🎬', '📷',
      '🚗', '✈️', '🚀', '🏠', '💰', '💸', '🛒', '☕', '🍺', '🍔', '🍕', '🍜', '🎂', '🍦', '⏰', '📱', '💻', '📚'
    ])
  ];

  /** 词 → emoji 联想(选词/上屏后在候选区追加;命中即提示)。 */
  private static buildWordEmoji(): Map<string, string[]> {
    const m = new Map<string, string[]>();
    m.set('开心', ['😄', '😁', '🥳']);
    m.set('高兴', ['😄', '😁']);
    m.set('哈哈', ['😂', '🤣']);
    m.set('笑', ['😄', '😂', '🤣']);
    m.set('爱你', ['❤️', '🥰', '😘']);
    m.set('爱', ['❤️', '😍']);
    m.set('喜欢', ['😍', '❤️']);
    m.set('难过', ['😢', '😭']);
    m.set('哭', ['😭', '😢']);
    m.set('生气', ['😠', '😡', '🤬']);
    m.set('谢谢', ['🙏', '😊']);
    m.set('加油', ['💪', '🔥']);
    m.set('棒', ['👍', '💯']);
    m.set('赞', ['👍', '💯']);
    m.set('好的', ['👌', '👍']);
    m.set('庆祝', ['🎉', '🎊']);
    m.set('生日', ['🎂', '🎉', '🎁']);
    m.set('吃饭', ['🍔', '🍜', '🍕']);
    m.set('睡觉', ['😴', '💤']);
    m.set('晚安', ['🌙', '😴']);
    m.set('早安', ['☀️', '😊']);
    m.set('太阳', ['☀️']);
    m.set('月亮', ['🌙']);
    m.set('狗', ['🐶']);
    m.set('猫', ['🐱']);
    m.set('花', ['🌸', '🌹', '🌺']);
    return m;
  }

  static readonly wordEmoji: Map<string, string[]> = EmojiData.buildWordEmoji();

  static associations(word: string): string[] {
    return EmojiData.wordEmoji.get(word) ?? [];
  }
}
