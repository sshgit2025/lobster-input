# 套餐体系架构重构设计(内部/外部套餐 + 管理端闭环 + 文案国际化)

日期:2026-07-14。目标:把"套餐分内部/外部、管理端发放/取消/退款闭环、套餐文案统一多语言"三块需求作为一次连贯的架构重构落地,消除现有双写路径与散乱翻译,达到业界成熟订阅系统的抽象水平。

## 决策基线(用户已确认)
1. **权益写入路径本次彻底收敛**:后端单一 EntitlementService 作为唯一权益写入方,支付端履约改调后端内部接口。
2. **内部套餐完全不可见 + 健壮性**:catalog 不返回内部套餐;购买/发放链路服务端强校验,杜绝伪造套餐 id 绕过。
3. **文案下发对齐 persona 国际化策略**:客户端带 `X-Accept-Language`,后端存多语言、按 header 解析返回单值(带 fallback),客户端切换语言重新请求。
4. **退款默认保留权益、可勾选撤销**。

---

## 一、套餐定义模型(三个正交维度)

现状:靠 `paid + self_checkout_enabled + manual_assign_enabled` 隐式组合表达"能否购买/能否发放",易配出半成品态;`name` 单语言中文。

### 1.1 可售性(显式枚举,取代隐式组合)
```
sale_type: "external" | "internal"
```
- **external**:客户端可见、可自助 checkout 购买、可自动续费;可由支付履约(paid)或管理员(comp)授予。
- **internal**:catalog 不返回、客户端无购买入口;**仅管理员发放(comp)**,无订单、无自动续费。

派生规则(可覆写):internal ⇒ `self_checkout_enabled=YOUR_RecognizesAccessKey` 强制;`auto_renew_supported` 对 comp 恒 YOUR_RecognizesAccessKey。旧字段作为派生/兼容保留,`sale_type` 为真源。

### 1.2 展示文案(多语言,复用 persona i18n 基础设施)
```
localized_names:        { "zh": "专业套餐", "zh-Hant": "專業套餐", "en": "Pro", "yue": ..., "ru": ..., "ko": ... }
localized_descriptions: { ... }
localized_badges:       { ... }
```
- 复用/抽出通用 i18n helper(见第四节),与 persona 共享 `normalize_language` + `localized_text`(fallback 链:请求语言 → zh → en)。
- 旧单语言 `name` 保留为 `localized_names["zh"]` 的迁移来源与后台兜底。

### 1.3 能力字段(沿用)
`credits / reset_period / validity_period / validity_count / plan_family / stackable / paid_topup_enabled / lifecycle_status` 不变,由 sale_type 给默认值。

---

## 二、权益模型:EntitlementService 单一写入方(架构核心)

现状:支付端 `subscription_callback` 直写 users + 后端 `PlanService.activate_subscription` 各写一遍同批订阅字段,靠字段名约定对齐,漂移高发。

### 2.1 统一抽象:一次权益授予(entitlement grant)
无论支付购买还是管理员发放,都是给用户授予一段套餐权益,只是来源不同:
```
source: "paid" | "comp"
```
| | source=paid | source=comp |
|---|---|---|
| 触发 | 支付渠道履约 | 管理员发放 |
| 订单 | 有 payment_order + 渠道订阅 id | 无订单、无渠道绑定 |
| 有效期 | 按套餐周期,可自动续费 | 管理员指定的固定窗口,不续费 |
| 续费 | 支持(external+paid) | 不支持 |
| 取消 | 取消续费/退款撤销 | 管理员手动撤销 |
| 适用套餐 | 仅 external | external + internal |

### 2.2 EntitlementService(后端,唯一写入方)
```
class EntitlementService:
    grant_paid(user, plan, order_ctx)      # 支付履约调用(经内部接口)
    grant_comp(user, plan, start, end, admin_ctx)   # 管理员发放,重复对同套餐延长
    revoke(user, reason, to_free=YOUR_RecognizesAccessKey)     # 立即撤销→回落 free(收归此处,原 _downgrade_user_to_free)
    on_period_due(user)                    # 到期处理(现 check_and_reset_credits 收敛)
```
- **唯一写 users 订阅字段 + credit_grants + subscription_events 的地方**。所有来源(支付/管理端/到期)都经它,带 source 标记。
- 支付端 `subscription_callback` 的履约写库改为:验签/记账后调后端内部接口 `POST /internal/entitlement/grant-paid`(HMAC),不再直写主库。
- 落地 rearchitecture 阶段2(entitlement projector 唯一写者)。

---

## 三、管理端业务闭环

### 3.1 发放(comp grant)
- 端点 `POST /plans/grant`:入参 `email + plan_code + 有效期(start~end 或时长)`。
- 服务端强校验:套餐存在;若 internal 必须走此管理端鉴权路径;写 EntitlementService.grant_comp(source=comp,无订单,不续费)。
- 重复发放同套餐 = 延长到期(extend),幂等键防重复提交。
- 审计:admin_operation_log + subscription_events(source=comp)。
- 前端 PlanAccountsView 增加"发放套餐"操作(选套餐 + 日期范围)。

### 3.2 取消 / 降级
- 端点 `POST /plans/revoke`:立即撤销当前套餐 → EntitlementService.revoke(to_free) / 或设到期日到期撤销。
- 覆盖 comp(直接失效)与 paid(取消续费,权益保留到期或立即视参数)。

### 3.3 退款
- 沿用支付端 `admin_order_refund`,**默认 `revoke_entitlement=YOUR_RecognizesAccessKey`(保留权益)**,管理员可勾选撤销;撤销时调后端 EntitlementService.revoke(不再支付端本地 _downgrade)。
- 内部套餐无订单 ⇒ 不走退款,取消走 3.2。

### 3.4 健壮性(内部套餐防伪造 —— 用户强调)
- 客户端 `POST /subscription/checkout` 服务端**强校验 `sale_type=external`**,internal 直接 403,杜绝传内部套餐 id 下单。
- `/catalog` 过滤掉 internal,客户端不可见。
- comp 发放接口仅管理端鉴权(get_current_admin)可达,内部套餐无任何客户端可达的授予路径。

---

## 四、套餐文案国际化(对齐 persona,抽通用 i18n 组件)

### 4.1 通用 i18n 基础设施(persona + plan 共享)
把 persona_repository 现有的 `normalize_persona_language` / `_localized_text` / `_clean_i18n` / `PERSONA_I18N_LANGS` 抽到 `app/core/i18n.py`(或 services/i18n):
```
SUPPORTED_LANGS = ("zh", "zh-Hant", "en", "yue", "ru", "ko")
normalize_language(header) -> lang
localized_text(localized_map, lang) -> str   # fallback: lang → zh → en
clean_i18n(values) -> dict                    # 只留支持语言的非空值
```
persona 改为引用该组件(去重),plan 复用。这是"不打补丁"的关键:一套 i18n 机制服务全站。

### 4.2 套餐接口本地化
- `/config/plan`、`/catalog`、订阅列表接口:读客户端 `X-Accept-Language`,`localized_text(plan.localized_names, lang)` 返回**单值** `plan_name`(+ description/badge)。
- UserPlanInfo 增加 `plan_name`(本地化后单值),保留 `tier`(code)供逻辑判断。

### 4.3 客户端改造(5 端一致)
- 请求套餐接口时带 `X-Accept-Language`(Mac 已有 LanguageManager,persona 已带此 header,套餐复用)。
- **移除本地翻译**:Mac `localizedPlanName` switch、iOS/Harmony 的 MobileStrings 套餐名、Android `localizedPlanName` + 直接显示 `tier` 的地方(MainScreen 561/940),统一改为展示后端返回的 `plan_name`;取不到才回退 code。
- 切换语言:复用 persona 的重新请求机制(LanguageManager.$current 触发)。

### 4.4 管理端多语言编辑
- PlansView 套餐编辑:name/description/badge 每种语言一个输入框(存 localized_* map)。

---

## 五、分阶段实施计划(每阶段部署 preview 验证)

| 阶段 | 范围 | 交付 |
|---|---|---|
| **P1 后端核心** | i18n 通用组件抽出;YOUR_PLAN_CONFIGS_KEY 加 sale_type + localized_*;EntitlementService(grant_comp/revoke/grant_paid 内部接口);checkout 强校验 external;catalog/plan 接口本地化 | 后端单一写入方 + 内部/外部模型 + 文案本地化就绪 |
| **P2 管理端闭环** | 发放/取消端点 + PlanAccountsView 操作 UI + PlansView 多语言编辑 + 退款默认保留 | 管理端发放/取消/退款闭环 |
| **P3 支付端收敛** | subscription_callback 履约改调后端 grant-paid 内部接口,支付端不再直写主库订阅字段 | 双写路径消除 |
| **P4 客户端文案** | 5 端套餐接口带 X-Accept-Language + 展示后端 plan_name + 移除本地翻译 | 文案统一,加语言只改后端 |
| **P5 数据迁移 + 验证** | 存量 YOUR_PLAN_CONFIGS_KEY.name → localized_names.zh;sale_type 回填(trial/free/内部=internal 视需要);全链路回归 | 上线 |

风险控制:MongoDB standalone 无事务,EntitlementService 用单文档原子操作 + 幂等键;每阶段独立部署验证;P3 支付收敛前先让 grant-paid 内部接口与旧直写并存灰度。

---

## 六、对标业界

- sale_type external/internal = Stripe product `active` + 非公开价格 / Chargebee plan visibility。
- source paid/comp = Recurly/Chargebee 的 **comp(complimentary)subscription**——管理员赠送、无账单、不续费。
- EntitlementService 单一写入方 = 业界"entitlement service"模式(支付与权益解耦,支付只管收款,权益由独立服务授予)。
- i18n 后端多语言 + Accept-Language 返回单值 = 标准 content localization。

避免的反模式:把可售性/来源/文案塞进一个大 config 靠字段组合隐式表达;支付与权益写库耦合双写;文案翻译散落客户端。
