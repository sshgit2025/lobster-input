# =============================================================================
# requirements.txt — 兼容备用（精确锁定版本）
#
# 推荐使用 uv 管理依赖（pyproject.toml + uv.lock），版本锁定更完整：
#   cd backend && uv sync
#
# 此文件为不使用 uv 的环境提供兼容支持，版本与 uv.lock 保持一致。
# 如需更新依赖，请先更新 pyproject.toml，执行 uv lock，再同步此文件。
# =============================================================================

# Web framework
fastapi==0.135.1
uvicorn[standard]==0.42.0
python-multipart==0.0.22
websockets==16.0

# LangChain core + providers
# UsageMetadata 在 1.0+ 统一为 TypedDict，字段: input_tokens / output_tokens / total_tokens
langchain==1.2.12
langchain-core==1.2.19
langchain-openai==1.1.11
langchain-anthropic==1.3.5
langchain-community==0.4.1

# LangGraph (prebuilt react agent)
langgraph==1.1.6
langgraph-checkpoint==4.0.2
langgraph-prebuilt==1.0.10
langgraph-sdk==0.3.13

# Config & validation
pydantic==2.12.5
pydantic-settings==2.13.1
python-dotenv==1.2.2

# OpenAI SDK (Whisper 语音识别 + LLM)
openai==2.28.0

# Database
motor==3.7.1
pymongo==4.16.0

# Auth & Email
python-jose[cryptography]==3.5.0
aiohttp==3.11.18

# Audio metadata (时长校验)
mutagen==1.47.0

# Search engine (Tavily, Agent 搜索意图)
tavily-python==0.7.23

# Date utilities (relativedelta for monthly credit reset)
python-dateutil==2.9.0.post0

# HTTP client (号池客户端 SDK)
httpx==0.28.1
requests==2.32.5

# ASR 用户词典发音纠偏
dashscope
jieba>=0.42.0
pypinyin>=0.54.0
rapidfuzz>=3.14.0
jellyfish>=1.1.0
wordfreq>=3.1.1,<4
cn2an==0.5.24
proces==0.1.7

# ASR 纠偏 token gate（短文本/单词语言识别 + Unicode Script 判断）
lingua-language-detector==2.2.0
regex==2026.2.28

# LLM 可观测性（LangWatch）
langwatch>=0.4.0

# Redis（队列/缓存）
redis>=5.0.0
