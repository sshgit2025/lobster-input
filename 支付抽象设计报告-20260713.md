# 支付渠道抽象扩展性审计 — lobster-input-payment

审计目标:评估"接入第三种支付渠道(支付宝直连 / Stripe / PayPal)"的改动量,找出所有阻碍"只改配置不改代码"的位置,给出务实可落地的抽象改进方案。

审计范围:`app/services/payment_providers/{base,creem,zpay,registry}.py`、`app/api/v1/payments.py`(2889 行)、`app/api/v1/webhook_admin.py`、`app/services/{webhook_ingest,YOUR_cache_key,subscription_lifecycle,runtime_config}.py`。

结论一句话:**adapter 层抽象只覆盖了"下单 + 验签 + 退款 + 取消"四个动作,而渠道的"能力差异"(是否代扣/是否折扣券/是否有 portal/webhook 事件形态/续费记账)全部以 `provider == "creem"` / `provider == "zpay"` 的形式硬编码散落在业务逻辑层。接入第三渠道不是"注册一个类 + 一条配置",而是要改 3 类代码、约 20+ 处分支。**

---

## 1. 现状抽象层能力矩阵

### 1.1 base.py 抽象接口(`PaymentProviderAdapter`,base.py:19-76)

| 抽象方法 | 签名位置 | 覆盖生命周期阶段 |
|---|---|---|
| `products_by_id()` | base.py:26 | 商品目录读取 |
| `create_checkout(...)` | base.py:29-43 | 首购/加购/升级下单 |
| `verify_webhook_signature(raw_body, signature)` | base.py:46 | webhook 验签 |
| `refund_payment(...)` | base.py:49-59 | 退款 |
| `cancel_subscription(subscription_id)` | base.py:61 | 取消订阅 |
| `product_by_id` / `product_details` | base.py:64-76 | 目录辅助(基类已实现) |

### 1.2 抽象接口没有覆盖的关键生命周期环节

| 缺失的抽象 | 现状 | 证据 |
|---|---|---|
| **webhook 事件规范化**(把 provider 原生事件 → 统一领域事件) | 完全没有。creem 走 `_dispatch_generic_webhook_event`,zpay 走 `_handle_zpay_notification`,各写各的 | payments.py:2562, 2458 |
| **续费(平台托管代扣)记账** | 没有抽象,creem 靠 `subscription.paid` webhook 二次进 `subscription_callback` | payments.py:2320, 2373 |
| **能力声明**(是否支持代扣/折扣券/portal/退款 API) | 没有任何 capability 字段,全靠业务层 `provider == "xxx"` 判定 | 见第 2 节 |
| **create_checkout 返回结构规范** | 无契约。creem 返回 Creem 原生 JSON,zpay 手工拼 `{id, checkout_url, status, ...}` | zpay.py:65-72 vs creem.py:127 |

### 1.3 Creem 特有、未进抽象基类的方法(仅在 CreemAdapter 上)

| 方法 | 位置 | 业务层如何调用 | 是否有抽象兜底 |
|---|---|---|---|
| `discount_by_code()` | creem.py:57 | payments.py:1031 用 `hasattr(adapter, "discount_by_code")` 探测 | 无,靠 duck-typing |
| `create_discount()` | creem.py:129 | payments.py:1549 升级补差价建券 | 无 |
| `delete_discount()` | creem.py:190 | payments.py:2271 支付后清理 | 无 |
| `customer_portal_link()` | creem.py:291 | payments.py:1781 自助管理页 | 无 |
| `cancel_subscription(subscription_id, mode, on_execute)` | creem.py:253 | 签名比基类**多了 mode/on_execute** 两参数 | 基类签名 base.py:61 只有 1 参数,子类偷偷加参 |

**问题**:`hasattr(adapter, "discount_by_code")`(payments.py:1028)是典型的"接口不完整,靠运行时探测"的反模式。新渠道即便实现了折扣能力,只要方法名不叫 `discount_by_code` 就接不上;基类 `cancel_subscription` 与子类签名不一致,是隐性的里氏替换违约。

### 1.4 ZPay 缺失能力如何在抽象层表达

- **代扣/自动续费**:抽象层**无法表达**。ZPay 无代扣,业务层用 `auto_renew = bool(data.auto_renew) and provider != "zpay"`(payments.py:1530)**硬编码关闭**。
- **退款**:ZPay 实现了 `refund_payment`(zpay.py:74),但底层易支付协议退款可靠性弱;抽象层没有"退款是否可用/是否需人工"的声明。
- **商品目录**:ZPay 无外部商品概念,`products_by_id()` 直接返回 `{}`(zpay.py:22-23),靠 `mode == "amount_order"` 动态金额下单绕过。这是**用价格行的 mode 字段间接表达能力**,是全仓唯一一处"配置驱动能力"的正例。

---

## 2. 散落在业务逻辑层的 provider 硬编码分支清单

> 判定标准:凡是在 `payments.py` / `webhook_admin.py` / `YOUR_cache_key.py` 里出现 `provider == "creem"/"zpay"/"apple"` 或渠道字面量分支的,都是接入新渠道时要改的地方。adapter 内部的 `self.code == ...` 不计。

### 2.1 payments.py(业务逻辑主战场)

| 行号 | 分支 | 语义 | 新渠道需要做什么 |
|---|---|---|---|
| 320 | `if provider != "creem": return ""` | 折扣码只对 creem 生效 | 若新渠道支持折扣要改 |
| 331 | `and provider == "creem"` | auto_apply 折扣仅 creem | 同上 |
| 336-339 | `_is_creem_discount_product_error` | 解析 Creem 特定错误串 | 新渠道错误语义不同,retry 逻辑失效 |
| 958 | `price.get("channel_code") == "creem"` | 折扣默认开关探测 | 硬编码渠道码 |
| 996 | `if channel.get("provider_code") != "creem": continue` | 折扣状态只查 creem | 折扣状态面板漏新渠道 |
| 1027 | `{**account, "provider_code": "creem", ...}` | 硬塞 creem | — |
| 1413 | `if order.get("provider") == "creem" and subscription_id` | 退款时立即取消订阅仅 creem | 新代扣渠道退款不联动取消 |
| **1530** | `auto_renew = ... and provider != "zpay"` | **代扣能力硬编码** | 每加一个无代扣渠道都要改这行 |
| **1537-1544** | `if provider == "creem" and mode==external_product ...` | **升级补差价分流:creem=折扣券 / 其它=动态金额** | 新渠道补差价策略要在此加分支 |
| 1560, 1576 | `"provider": "creem"` | upgrade_discount 台账渠道字面量 | — |
| 1606 | `_is_auto_discount(...) and _is_creem_discount_product_error(...)` | 折扣失败回退全价 | creem 专属 |
| 1774 | `if provider != "creem" or not customer_id` | portal 仅 creem | 新平台托管渠道要改 |
| 1809 | `if provider == "apple"` | Apple 托管续费拒绝本地取消 | 平台托管渠道各写各的 |
| 1813 | `if provider == "creem" and subscription_id` | 取消续费同步渠道仅 creem | 新代扣渠道要改 |
| 2230-2260 | `_handle_creem_checkout_completed` | creem 加购完成 handler | — |
| 2267 | `disc.get("provider") != "creem"` | 折扣券清理 | — |
| 2288 | `old_provider != "creem"` | 取消旧代扣订阅仅认 creem | 新代扣渠道换渠道会双扣 |
| 2320-2407 | `_handle_creem_subscription_paid` | creem 订阅支付+续费 | 新平台托管渠道要复制整套 |
| 2410-2449 | `_handle_creem_subscription_status` | creem 订阅状态事件 | 同上 |
| 2458-2527 | `_handle_zpay_notification` | zpay 通知处理 | 新单笔渠道要复制 |
| 2573-2579 | `if event_type == "checkout.completed" / "subscription.paid" / ...` | **creem 事件类型路由**(硬编码事件名) | 新渠道事件名不同,整段不复用 |
| **2588-2640** | `payment_provider_webhook` 内 `if provider == "zpay"` × 5 处(2602/2611/2628/2630/2638) | **webhook 摄取入口按 provider 二分** | 每加渠道要在入口加 elif |

### 2.2 webhook_admin.py(失败事件重放)

| 行号 | 分支 |
|---|---|
| 22 | `REPLAYABLE_PROVIDERS = {"creem", "zpay", "apple"}` 字面量集合 |
| 93-99 | `_replay_zpay_event` |
| 102-106 | `_replay_creem_event` |
| 125-130 | `if provider == "apple" / elif "zpay" / else creem` 重放分发 |

### 2.3 YOUR_cache_key.py(配置与默认值)

| 行号 | 分支 |
|---|---|
| 14-17 | `CHANNEL_PROVIDER_DEFS` 硬编码 creem/zpay + `order_mode`(**且这个常量实际未被 clean/load 使用,是死配置**) |
| 86-91 | `default_YOUR_cache_key` 里 creem/zpay 的默认 payment_methods / channels 字面量 |

### 2.4 registry.py

| 行号 | 分支 |
|---|---|
| 10-13 | `_ADAPTER_CLASSES = {creem, zpay}` 手工字典 —— **这里是唯一"符合插件注册模式"的地方**,加类要改一行,可接受 |

**统计:接入第三渠道需触碰约 20+ 处分支,横跨 payments.py / webhook_admin.py / YOUR_cache_key.py 三个业务文件,registry.py 反而是最轻的。**

---

## 3. webhook 处理的扩展性

### 3.1 现状:有半个统一入口,但分发仍硬编码

- **统一的幂等摄取管道存在**:`webhook_ingest.py` 提供 `ingest / mark_done / mark_failed`,以 `(provider, event_id)` 唯一索引去重(webhook_ingest.py:69, 63)。这一层是 provider 无关的,**设计良好**。
- **但摄取入口 `payment_provider_webhook`(payments.py:2588)之上全是 provider 特判**:
  - 验签 body 预处理:`_zpay_notification_body(...) if provider == "zpay" else raw_body`(2602)
  - 事件 ID 提取:zpay 用 `_zpay_webhook_event_id`,其它用 `_generic_webhook_event`(2611-2617)
  - 处理分发:`if provider == "zpay": _handle_zpay_notification else _dispatch_generic_webhook_event`(2630-2633)
  - 应答格式:`PlainTextResponse("success") if provider == "zpay"`(2628, 2638)
- **creem 系事件路由本身又是硬编码事件名**:`_dispatch_generic_webhook_event`(2562)里 `checkout.completed` / `subscription.paid` / `subscription.*` 全是 Creem 事件字符串(2573-2577)。

### 3.2 结论

没有 **per-provider handler 注册机制**,也没有 **provider 事件 → 统一领域事件的规范化层**。新渠道 webhook 接入要:(1) 在入口 `payment_provider_webhook` 加 `elif provider == "xxx"` 处理 body/event_id/应答;(2) 写一整套 `_handle_xxx_*` handler;(3) 在 webhook_admin 的重放里再加一遍分发。**同一套渠道逻辑被要求在两个地方(实时入口 + 重放)各写一次**,是重复且易漂移的。

---

## 4. 自动续费(renew)闭环与记账 —— 重点确认

### 4.1 结论确认:续费**不创建 payment_orders 订单记录** ✅ 属实

证据链:

1. `payment_orders` 全仓只在两处 `insert_one`:
   - 订阅 checkout:payments.py:1635
   - 加购 checkout:payments.py:1923
   两者都在**下单(checkout)流程**里。续费没有 checkout。
2. 续费路径:Creem 代扣 → `subscription.paid` webhook → `_handle_creem_subscription_paid`(2320)→ `subscription_callback`(2373),`charge_type=CHARGE_TYPE_AUTO_RENEWAL`(2386,因为续费无 checkout,`not checkout` 为真)。
3. `subscription_callback` 的 **renew 分支(payments.py:2013-2058)**:只做
   - `db["users"].update_one(...)` 延权益到期时间(2018-2044)
   - `CreditGrantLifecycleCoordinator.apply_subscription_activation`(2045)
   - `_mark_event_applied`(2057)→ 写 `payment_callback_events`
   **全程没有 `payment_orders` 写入**。
4. `_mark_checkout_completed`(2197)是唯一把 `payment_orders` 置为 `status="paid"` 的地方(2216),但它 `if not checkout: return`(2198),续费无 checkout,**根本不会进来**。`_handle_creem_subscription_paid` 里 `if checkout:` 才调它(2391),续费分支 checkout 为 None。

### 4.2 (a) 续费缺 payment_orders 对管理端/对账/收入统计的影响

- 管理端订单列表 `admin_orders`(payments.py:1065-1066)直接 `db["payment_orders"].find(...)`。**续费扣款不会出现在订单列表**。
- 财务对账/收入统计若以 `payment_orders` 为准 → **每一笔 Creem 续费收入都不在账**。用户第 2、3、4... 期的钱收了,订单表看不到。
- 退款也受影响:`admin_order_refund`(1360)以 `payment_orders` 为入口,**续费产生的扣款无法从管理端退款**(没有对应 order)。

### 4.3 (b) payment_callback_events 是否算完整记录

- 续费确实在 `payment_callback_events` 留了记录(`_record_event` @ 2010,event_type="subscription",带 `paid_amount_cents` / `charge_type` / `provider_payment_id` / `payment_channel`),`_mark_event_applied` 回写 result。
- **但它是"事件流水/审计日志",不是"业务订单"**:没有 `order_id`、没有统一的 `status=paid`、字段命名与 `payment_orders` 不一致(`payment_order_id` 只是回退成 `payment_event_id`,2003)。管理端所有订单类查询/退款/导出都是面向 `payment_orders` 的,不消费 `payment_callback_events` 做订单展示。
- 结论:**有金额有类型,但不是可对账、可退款、可展示的业务订单**。等于收入闭环缺一环。

### 4.4 (c) 首购 / 续费 / 升级三场景订单完整性对比

| 场景 | 有 checkout? | payment_orders? | callback_events? | 订单闭环 |
|---|---|---|---|---|
| **首购**(手动) | 是(1635) | 是,`paid`(2216) | 是 | ✅ 完整 |
| **升级**(手动,补差价) | 是(1635,带 upgrade_credit_cents) | 是,`paid` | 是 | ✅ 完整 |
| **续费**(Creem 平台托管代扣) | **否** | **否** | 是(仅事件) | ❌ **无订单** |
| 加购积分(手动) | 是(1923) | 是,`paid` | 是 | ✅ 完整 |

**只有"平台托管自动续费"这一类会掉出订单闭环**,恰恰是长期最高频、最需要对账的收入来源。

### 4.5 (d) 未来平台托管续费新渠道(如 Stripe)会不会重蹈覆辙

**会,而且是结构性的必然**。因为缺陷根因是"payment_orders 只在 checkout 流程建,续费无 checkout 就没订单",这是与 provider 无关的架构缺口。Stripe `invoice.paid`、PayPal 订阅周期扣款等任何"平台托管代扣"都会复现同一问题——除非在 `subscription_callback` 的 renew 分支补建订单(见第 7 节方案 C)。

---

## 5. 配置驱动程度评估

### 5.1 已经配置化的部分(做得好的)

- 渠道启用/密钥/账号:`YOUR_BILLING_CONFIG_KEY`(system_config)里 `channels[].accounts[]`,含 `api_key/webhook_secret/merchant_id/api_base_url/environment`(YOUR_cache_key.py:185-203)。**多账号、密钥、环境已完全配置驱动**。
- 费率/币种:`currencies[]` + `channel_prices[]`,支持 `pricing_strategy=fixed/exchange_rate`、汇率换算(YOUR_cache_key.py:341-376)。**定价配置驱动,做得好**。
- 下单模式:`channel_prices[].mode = external_product | amount_order`(YOUR_cache_key.py:233-235)。**这是唯一一处"能力用配置字段表达"的正例**——外部商品制 vs 动态金额制由配置决定,不看 provider 字面量。
- 回调密钥/service_url:`YOUR_PAYMENT_RUNTIME_CONFIG_KEY`(runtime_config.py)。

### 5.2 仍然硬编码的能力(阻碍"只改配置")

| 能力维度 | 现状 | 位置 |
|---|---|---|
| 是否支持代扣/自动续费 | 硬编码 `provider != "zpay"` | payments.py:1530 |
| 是否支持折扣券 | 硬编码 `provider == "creem"` | payments.py:320, 331, 958, 996 |
| 是否有 customer portal | 硬编码 `provider != "creem"` | payments.py:1774 |
| 升级补差价策略 | 硬编码 creem=券 / else=动态金额 | payments.py:1537 |
| 平台托管续费 vs 手动 | 硬编码(创建续费 handler 时判定) | payments.py:1809, 2288 |
| webhook 事件形态/应答格式 | 硬编码 provider 分支 | payments.py:2602-2638 |
| 退款是否走 API | 隐式(有无实现 refund_payment) | — |

### 5.3 与理想目标的差距

理想:**"注册一个 adapter 类 + 在 YOUR_cache_key 加一条 channel 记录"** 即接入完成。
当前差距:adapter 类注册确实只需改 registry.py 一行(✅),channel 配置也支持(✅),**但能力差异全部在业务层硬编码,导致新增一条配置不足以让业务逻辑正确分流**。距离目标还差一个**"provider capability 声明 + 业务层改为读能力而非读 provider 名"**的中间层。

---

## 6. 对标业界成熟方案的差距

对标 Stripe(PaymentIntent/Subscription)、以及通用支付网关的 provider-plugin 模式:

| 业界关键抽象 | 当前是否具备 | 差距 |
|---|---|---|
| **统一订单状态机**(pending → paid → refunding → refunded,续费也产订单) | 部分(payment_orders 有 status,但续费不产订单) | 续费掉出状态机;状态流转散在各 handler |
| **Provider Capability 声明**(supports_recurring / supports_discount / supports_portal / supports_refund_api / order_mode) | ❌ 无(仅 mode 一个维度进了配置) | 全部靠 `provider == "xxx"` |
| **Webhook 事件规范化层**(provider 原生事件 → 统一领域事件 `PaymentEvent{type: purchase/renewal/refund/cancel, amount, subscription_id, ...}`) | ❌ 无 | creem/zpay 各自直连业务函数,事件名硬编码 |
| **统一 checkout 返回契约**(标准 `CheckoutResult` dataclass) | ❌ 无(返回裸 dict,字段各异) | 业务层 `checkout.get("id")` / `.get("checkout_url")` 靠约定 |
| **续费/代扣抽象**(recurring engine 与 provider 解耦) | ❌ 无 | Creem 续费逻辑与 handler 焊死 |
| 幂等事件台账 | ✅ webhook_ingest 有 | 这块与业界持平 |
| 多账号/多环境/密钥配置化 | ✅ 有 | 持平甚至更细 |

**核心缺失的四个抽象:capability 声明、webhook 事件规范化、统一 checkout/事件契约、续费记账解耦。**

---

## 7. 目标抽象设计方案(务实可落地)

设计原则:**不推倒重来**。现有 registry 插件化、webhook_ingest 幂等台账、YOUR_cache_key 多账号配置都保留,只补三层缺失抽象,把散落的 `provider == "xxx"` 收敛成"读能力"。

### 方案 A:Provider Capability 声明机制(工作量:小,收益:大)

在 `base.py` 给 `PaymentProviderAdapter` 增加类级能力声明(默认全 YOUR_RecognizesAccessKey,子类覆盖):

```python
class PaymentProviderAdapter:
    code = "unknown"
    display_name = "Unknown"
    # capability 声明
    supports_recurring = YOUR_RecognizesAccessKey      # 平台托管代扣/自动续费
    supports_discount_codes = YOUR_RecognizesAccessKey # 折扣券
    supports_customer_portal = YOUR_RecognizesAccessKey
    supports_refund_api = YOUR_RecognizesAccessKey
    order_mode = "amount_order"     # external_product | amount_order
    settlement_currencies = ()      # 空=不限
```

- CreemAdapter:`supports_recurring/discount/portal/refund_api = YOUR_RecognizesAccessKey`,`order_mode="external_product"`。
- ZPayAdapter:全 YOUR_RecognizesAccessKey,`order_mode="amount_order"`,`settlement_currencies=("CNY",)`。
- 业务层把 `provider != "zpay"` 改成 `adapter.supports_recurring`,`provider == "creem"`(折扣)改成 `adapter.supports_discount_codes`,portal 判定改 `adapter.supports_customer_portal`。
- 把 base 里签名不一致的 `cancel_subscription` 补上 `mode/on_execute` 默认参数,消除 LSP 违约;把 `discount_by_code/create_discount/delete_discount/customer_portal_link` 提为 base 上 `raise NotImplementedError` 的抽象方法,`hasattr` 探测(payments.py:1028)改为 `adapter.supports_discount_codes`。

**改动量**:base.py +15 行;payments.py 约 8 处 `provider == "xxx"` 判定改为读 capability(1530/320/331/958/996/1774/1537 等)。**这一步就把"加无代扣/无折扣新渠道"从改代码变成子类声明。**

### 方案 B:统一 Webhook 事件规范化 + handler 注册(工作量:中,收益:大)

给 adapter 增加规范化方法,把 provider 原生 webhook → 统一领域事件:

```python
@dataclass
class NormalizedPaymentEvent:
    kind: str        # "purchase" | "renewal" | "refund" | "subscription_status" | "ignored"
    event_id: str
    ack_body: str    # provider 要求的应答("success" / "" / json)
    payload: dict    # email, plan/amount, subscription_id, paid_amount_cents...

class PaymentProviderAdapter:
    def webhook_verify_body(self, raw_body, request) -> bytes: return raw_body  # zpay 覆盖
    def normalize_webhook_event(self, raw_body, request) -> NormalizedPaymentEvent: ...
```

- `payment_provider_webhook`(2588)瘦身为:取 accounts → `adapter.webhook_verify_body` 验签 → `webhook_ingest.ingest`(保留)→ `adapter.normalize_webhook_event` → 按 `event.kind` 分发到 **provider 无关的领域处理器** `handle_purchase / handle_renewal / handle_refund / handle_subscription_status`。
- 把现有 `_handle_creem_subscription_paid` / `_handle_zpay_notification` 的**业务部分**(建/延权益、记账)抽进领域处理器;把 creem/zpay 事件解析部分下沉进各自 adapter 的 `normalize_webhook_event`。
- webhook_admin 重放(webhook_admin.py:125-130)改为同样调 `adapter.normalize_webhook_event` + 领域处理器,**消除"实时入口与重放各写一遍"**。

**改动量**:base +契约;creem/zpay adapter 各 +1 normalize 方法(搬移现有解析);payments.py webhook 入口与 handler 重构约 150 行;webhook_admin 分发简化。**接入新渠道 webhook 从此 = 实现 normalize_webhook_event,不再动入口。**

### 方案 C:续费订单补建(工作量:小,收益:大 —— 直接补收入闭环缺陷)

在 `subscription_callback` 的 renew 分支(payments.py:2013)补建 `payment_orders`:

- 在 renew 分支内 `insert_one` 一条 `payment_orders`:`kind="subscription"`、`charge_type="auto_renewal"`、`status="paid"`、`order_id=f"ord_renew_{event_id}"`、`amount_cents=paid_amount`、`provider/payment_channel/provider_payment_id` 取自 event_payload、`paid_at=now`。用 `order_id` 唯一或 upsert 保证幂等(webhook 会重投)。
- 这样管理端订单列表、收入统计、退款入口对续费一视同仁。
- **同时修复了方案的 (d):任何平台托管续费新渠道(Stripe/PayPal)只要复用 `subscription_callback` renew 分支,自动获得订单记录**,不会重蹈覆辙。

**改动量**:payments.py renew 分支 +1 个 upsert(约 20 行)。**这是投入产出比最高的一处修复,建议优先做。**

### 方案 D:配置补齐 + 清理死配置(工作量:小)

- `YOUR_cache_key.py` 的 `CHANNEL_PROVIDER_DEFS`(14-17)当前是死配置(未被 clean/load 消费)。要么删掉,要么改成"渠道能力默认值来源",与方案 A 的 capability 对齐(provider 注册时从 adapter 类读 capability 回填到 channel,前端展示"该渠道支持代扣/折扣"等)。
- `default_YOUR_cache_key`(86-91)的 creem/zpay 字面量可保留(仅首次初始化用),但新渠道不应要求改这里——靠管理端配置新增 channel 即可。

### 落地优先级建议

1. **方案 C(续费补单)** —— 独立、低风险、直接补收入闭环资损隐患,先做。
2. **方案 A(capability 声明)** —— 低风险,把 8+ 处 `provider == "xxx"` 收敛,为后续接入铺路。
3. **方案 B(webhook 规范化 + 领域处理器)** —— 改动最大但价值最高,消除重放重复与入口硬编码,建议在有第三渠道明确需求时做。
4. **方案 D(清理死配置)** —— 顺手做。

完成 A+B+C 后,接入第三渠道的目标态 = **"registry 注册一个 adapter 类(声明 capability + 实现 create_checkout/normalize_webhook_event/refund) + 管理端加一条 channel 配置"**,业务层 payments.py 零改动。

---

## 附:接入第三渠道"现状"改动清单(未采纳方案前)

以接入 **Stripe(平台托管订阅 + 折扣 + portal + 代扣)** 为例:

1. `app/services/payment_providers/stripe.py` 新建 adapter(必须)。
2. `registry.py:10-13` 注册类(一行,可接受)。
3. `YOUR_cache_key.py:14-17, 86-91` 加 provider 默认定义。
4. `payments.py:1530` 代扣能力:加 `provider == "stripe"` 视为支持续费。
5. `payments.py:320/331/958/996/1606` 折扣:加 stripe 分支。
6. `payments.py:1537-1544` 升级补差价:加 stripe 策略分支。
7. `payments.py:1774` portal:加 stripe。
8. `payments.py:1809/1813/2288/1413` 取消续费/退款联动取消:加 stripe。
9. `payments.py:2320-2449` 复制一套 `_handle_stripe_subscription_paid / _status`,或改 `_dispatch_generic_webhook_event`(2562-2585)的事件名路由(Stripe 事件名 `invoice.paid` ≠ `subscription.paid`)。
10. `payments.py:2588-2640` webhook 入口加 stripe 分支(body/event_id/应答)。
11. `webhook_admin.py:22/102-130` 重放加 stripe。
12. **续费仍不产 payment_orders(第 4 节缺陷),需额外做方案 C**。

**合计:1 个新文件 + 约 20 处业务分支改动 + 一个结构性续费记账缺陷。**
