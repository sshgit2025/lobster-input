<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
                    xmlns:win="clr-namespace:System.Windows;assembly=PresentationFramework">

    <Style x:Key="WindowsChromeButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="46" />
        <Setter Property="Height" Value="38" />
        <Setter Property="FontFamily" Value="{StaticResource IconFont}" />
        <Setter Property="FontSize" Value="10" />
        <Setter Property="Foreground" Value="{DynamicResource DsFgMutedBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="shell:WindowChrome.IsHitTestVisibleInChrome" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonChrome"
                            Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonChrome" Property="Background" Value="{DynamicResource DsBgHoverBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource DsFgBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonChrome" Property="Background" Value="{DynamicResource DsBgActiveBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="WindowsChromeCloseButtonStyle" TargetType="Button" BasedOn="{StaticResource WindowsChromeButtonStyle}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonChrome"
                            Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonChrome" Property="Background" Value="#FFE81123" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonChrome" Property="Background" Value="#FFC50F1F" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="WindowsChromeMaximizeButtonStyle" TargetType="Button" BasedOn="{StaticResource WindowsChromeButtonStyle}">
        <Setter Property="Command" Value="{x:Static win:SystemCommands.MaximizeWindowCommand}" />
        <Setter Property="Content" Value="&#xE922;" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="Maximized">
                <Setter Property="Command" Value="{x:Static win:SystemCommands.RestoreWindowCommand}" />
                <Setter Property="Content" Value="&#xE923;" />
            </DataTrigger>
            <DataTrigger Binding="{Binding CommandParameter, RelativeSource={RelativeSource Self}}" Value="NoResize">
                <Setter Property="IsEnabled" Value="False" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="WindowsChromeWindowStyle" TargetType="Window">
        <Setter Property="WindowStyle" Value="None" />
        <Setter Property="ResizeMode" Value="CanResize" />
        <Setter Property="Background" Value="{DynamicResource DsBgBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource DsFgBrush}" />
        <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
        <Setter Property="shell:WindowChrome.WindowChrome">
            <Setter.Value>
                <shell:WindowChrome CaptionHeight="38"
                                    GlassFrameThickness="0"
                                    NonClientFrameEdges="None"
                                    CornerRadius="{StaticResource RadiusWindow}"
                                    ResizeBorderThickness="6"
                                    UseAeroCaptionButtons="False" />
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Window">
                    <Border Background="{DynamicResource DsBgBrush}"
                            BorderThickness="0"
                            SnapsToDevicePixels="True"
                            UseLayoutRounding="True">
                        <Grid SnapsToDevicePixels="True" UseLayoutRounding="True">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="38" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0"
                                    Background="{DynamicResource DsBgSunkenBrush}"
                                    BorderThickness="0"
                                    SnapsToDevicePixels="True">
                                <Grid>
                                    <TextBlock Text="{TemplateBinding Title}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               FontSize="12"
                                               FontWeight="Medium"
                                               Foreground="{DynamicResource DsFgMutedBrush}" />
                                    <StackPanel Orientation="Horizontal"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Top"
                                                shell:WindowChrome.IsHitTestVisibleInChrome="True">
                                        <Button x:Name="MinimizeButton"
                                                Style="{StaticResource WindowsChromeButtonStyle}"
                                                Command="{x:Static win:SystemCommands.MinimizeWindowCommand}"
                                                CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                                Content="&#xE921;" />
                                        <Button x:Name="MaximizeButton"
                                                Style="{StaticResource WindowsChromeMaximizeButtonStyle}"
                                                Tag="{Binding WindowState, RelativeSource={RelativeSource TemplatedParent}}"
                                                CommandParameter="{Binding ResizeMode, RelativeSource={RelativeSource TemplatedParent}}"
                                                CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}" />
                                        <Button x:Name="CloseButton"
                                                Style="{StaticResource WindowsChromeCloseButtonStyle}"
                                                Command="{x:Static win:SystemCommands.CloseWindowCommand}"
                                                CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                                Content="&#xE8BB;" />
                                    </StackPanel>
                                    <Rectangle Height="1"
                                               VerticalAlignment="Bottom"
                                               Fill="{DynamicResource DsLineStrongBrush}"
                                               IsHitTestVisible="False"
                                               SnapsToDevicePixels="True" />
                                </Grid>
                            </Border>
                            <AdornerDecorator Grid.Row="1">
                                <ContentPresenter x:Name="PART_Content"
                                                  Content="{TemplateBinding Content}" />
                            </AdornerDecorator>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
