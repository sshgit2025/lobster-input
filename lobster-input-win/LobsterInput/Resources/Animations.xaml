<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Storyboard x:Key="FadeInStoryboard">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0"
                         To="1"
                         Duration="{StaticResource DurationFade}"
                         EasingFunction="{StaticResource EaseStandard}" />
    </Storyboard>

    <Storyboard x:Key="SlideUpStoryboard">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0"
                         To="1"
                         Duration="{StaticResource DurationSlide}"
                         EasingFunction="{StaticResource EaseStandard}" />
    </Storyboard>

    <Storyboard x:Key="ScaleInStoryboard">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0"
                         To="1"
                         Duration="{StaticResource DurationScale}"
                         EasingFunction="{StaticResource EaseStandard}" />
    </Storyboard>

    <Storyboard x:Key="PulseStoryboard" RepeatBehavior="Forever" AutoReverse="True">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="1"
                         To="0.5"
                         Duration="{StaticResource DurationScale}"
                         EasingFunction="{StaticResource EaseStandard}" />
    </Storyboard>

    <Storyboard x:Key="WaveStoryboard" RepeatBehavior="Forever" AutoReverse="True">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0.45"
                         To="1"
                         Duration="{StaticResource DurationFast}"
                         EasingFunction="{StaticResource EaseStandard}" />
    </Storyboard>

    <Storyboard x:Key="SpinFullStoryboard">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                         From="0"
                         To="360"
                         Duration="0:0:0.7" />
    </Storyboard>

</ResourceDictionary>
