<Window x:Class="LobsterInput.Views.AgreementView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Agreement"
        Icon="pack://application:,,,/LobsterInput;component/Resources/Images/lobster.ico"
        Style="{StaticResource WindowsChromeWindowStyle}"
        Width="680"
        Height="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Background="{DynamicResource DsBgBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="1"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="28,20,28,16">
            <TextBlock x:Name="TitleText"
                       FontFamily="{StaticResource AppFont}"
                       FontSize="{StaticResource FontSizePageTitle}"
                       FontWeight="{StaticResource FontWeightSemiBold}"
                       Foreground="{DynamicResource DsFgBrush}"/>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource DsDividerStyle}"/>

        <Grid Grid.Row="2">
            <StackPanel x:Name="LoadingPanel"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Visibility="Collapsed">
                <ProgressBar IsIndeterminate="True"
                             Style="{StaticResource DsIndeterminateSpinnerStyle}"
                             Width="32"
                             Height="32"
                             HorizontalAlignment="Center"/>
                <TextBlock x:Name="LoadingText"
                           FontSize="{StaticResource FontSizeDescription}"
                           Foreground="{DynamicResource DsFgMutedBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,12,0,0"/>
            </StackPanel>

            <StackPanel x:Name="ErrorPanel"
                        Width="360"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Visibility="Collapsed">
                <TextBlock Text="&#xE783;"
                           FontFamily="{StaticResource IconFont}"
                           FontSize="24"
                           Foreground="{DynamicResource DsDangerBrush}"
                           HorizontalAlignment="Center"/>
                <TextBlock x:Name="ErrorText"
                           FontSize="{StaticResource FontSizeDescription}"
                           Foreground="{DynamicResource DsFgMutedBrush}"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           Margin="0,12,0,16"/>
                <Button x:Name="RetryBtn"
                        Style="{StaticResource DsButtonPrimaryStyle}"
                        Click="RetryBtn_Click"
                        Width="120"
                        HorizontalAlignment="Center"/>
            </StackPanel>

            <FlowDocumentScrollViewer x:Name="ContentViewer"
                                      Padding="28,16"
                                      VerticalScrollBarVisibility="Auto"
                                      Visibility="Collapsed"
                                      Foreground="{DynamicResource DsFgBrush}"
                                      Background="Transparent"
                                      IsToolBarVisible="False"/>
        </Grid>

        <Border Grid.Row="3" Style="{StaticResource DsDividerStyle}"/>

        <Border Grid.Row="4" Padding="28,14">
            <Button x:Name="CloseBtn"
                    Style="{StaticResource DsButtonDefaultStyle}"
                    Click="CloseBtn_Click"
                    Width="120"
                    HorizontalAlignment="Right"/>
        </Border>
    </Grid>
</Window>
