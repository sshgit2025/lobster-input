# lobster-input 计费域全量审计报告

审计范围:后端(lobster-input-backend)、管理端(lobster-input-admin)、支付端(lobster-input-payment)、号池端(lobster-input-api-manage)的**套餐、订阅、支付、积分、扣费、配置**模块全量代码。
审计日期:2026-07-13。基线:backend @main `813adf9`、admin @main `f7e28af`、payment @master `03080aa`、api-manage @main `549c574`。
方法:6 路多智能体静态深审 + git 演进史比对 + preview 业务服务器(192.0.2.14)运行时数据实证。MongoDB 为 **standalone(非 replica set,不支持多文档事务)**——这是所有竞态类修复方案的硬约束。

---

## 一、执行摘要

计费重构(2026-07-12/13 的阶段0/1/A/B/D + 收尾 M1/M2/M3)整体方向正确、纪律良好:YOUR_PLAN_CONFIGS_KEY 双写回环已消除、金额全程整数分、webhook 双层幂等、服务端权威定价、跨渠道防双扣、Apple 交易归属绑定等关键设计扎实。**但重构存在两类系统性欠账**:①渠道化改造后 2026-05 的 mock 自动续费遗留路径未删,形成正在生效的资损;②rearchitecture 阶段2(entitlement projector 唯一写者)、阶段3(规则收敛单模块)"文档写了代码没动",导致 users/credit_grants 多写者竞态与后端/支付端双实现漂移。

| 级别 | 数量 | 说明 |
|---|---|---|
| P0 资损 | 2 | 零元自动续费(已实证发生)、Creem 折扣升级全价记账 |
| P1 业务/安全缺陷 | 11 | 实时ASR白嫖、支付掉单无对账、跨订阅状态覆盖、多写者竞态、内部端点公网可达等 |
| P2 质量/隐患 | 约 14 | 僵尸镜像代码、双实现漂移、幂等占位符、死集合、Jinja半下线等 |
| P3 建议 | 约 18 | 弱默认加固、时区粒度、误导文案、文档漂移等 |

---

## 二、运行时实证结论(主审计员亲验)

1. **P0 零元续费已在生产库真实发生**:voice_input.subscription_purchase_events 有 1 条 `charge_type=auto_renewal / price_cents=0 / provider=zpay / status=applied`(用户 developer@example.com pro 套餐),而 zpay 渠道设计上无代扣能力 → 白嫖实锤。preview 23 用户 / 2 人开自动续费。
2. **安全弱默认 preview 已缓解**:各服务 .env 已覆盖 SECRET_KEY/JWT_SECRET_KEY/INTERNAL_API_KEY(admin、api-pool、backend 均已配),故"JWT 弱默认可伪造令牌"在 preview **不成立**,降级为代码 fail-fast 加固项(生产/新部署漏配才高危)。
3. **内部端口公网暴露**:27017(MongoDB)、8000、8888、8889、8890 全部 bind 0.0.0.0 且公网 nc 可连;MongoDB auth 已开(需密码)。运维加固项。
4. **支付端内部 admin 端点经网关公网可达**:`https://example.com/lobster/payment/api/v1/payments/admin/config` 返回 422(穿透到 FastAPI 参数校验),退款端点同理;对比号池 pool 端点已 nginx return 403。
5. **backend→admin provider 配置链路**靠 backend.API_KEY == admin.BACKEND_API_KEY 巧合可用(两侧都漏配专用密钥走回退),当前不坏但脆弱。
6. **P0-2 Creem 折扣升级**:preview 所有 cycle_upgrade 订单停在 pending_payment、无成功升级回调、无 discount 订单 → 缺生产数据佐证,且 Creem 真实 webhook 签名无法在 preview 伪造,保留为代码逻辑级 P0。

---

## 三、P0 — 高危 / 资损(立即修)

### P0-1 零元"自动续费":到期本地免费续期,无真实扣款【已实证发生】
- 位置:`backend/app/services/billing/subscription_purchase_service.py:271-425`(`apply_due_auto_charge` 硬编码 `price_cents=0`、provider 兜底 `mock_auto`、`payment_channel=auto_debit`,全程无任何支付服务调用);触发链 `plan_service.py:465-477 _try_due_auto_charge`(仅校验 `subscription_auto_renew` 无渠道 gate)← `plan_service.py:307-322 check_and_reset_credits`(被 `credit_account_service.py:34` 每次扣费/查余额惰性触发 + `main.py:134 process_due_resets` 后台周期扫描);使能端点 `payments.py:1115-1136 /payments/subscription/auto-renew`(只翻本地标志、不做渠道拉齐、对 zpay 也放行)。
- 影响:①任意付费用户开自动续费即被后端每周期零元续期,无限白嫖;②Creem 卡扣失败 past_due 本地照样免费续期,打穿渠道 dunning;③Creem 真实续费 webhook + 本地零元续期叠加 → 双倍权益期。
- 来源:2026-05 mock 计费生命周期(`7933c6e`)在渠道化重构后未删。
- 修复:删除/熔断 `apply_due_auto_charge` 的本地权益发放,续费权益只认支付渠道回调;`/payments/subscription/auto-renew` 端点比照 `plan_admin.py:392-405` 补渠道校验(仅 creem 等有代扣能力渠道可开);清查并冲正已产生的 `auto:auto_renewal:*` 零元事件与被误续期的用户权益。
- 附:`plan_service.py:469` 为函数内 import,违反项目"import 置顶"规约。

### P0-2 Creem 折扣升级用商品目录全价记账,退款/补差价双向超额出账【代码逻辑级,需生产监控】
- 位置:`payment/app/api/v1/payments.py:2337`(`paid_amount_cents=int(product.get("price") or 0)` 用全价而非实扣)→ `_mark_checkout_completed`(:2187)写入 `payment_orders.paid_amount_cents`。
- 资损链:①退款 `_resolve_refund_amount`(:1226)以 paid_amount_cents 为基数 → 用户只付差价却按全价退;②后续升级补差价基数 `_upgrade_credit_cents`(:729-731)用上一笔 paid_amount_cents → 抵扣被放大,可累积。
- 边界:ZPay 用实付 money(:2417)不受影响;非升级 Creem 购买全价==实扣不受影响;仅 Creem 折扣升级单受影响(恰是差额最大场景)。
- 修复:Creem 升级履约时 `paid_amount_cents` 取回调实扣额(需确认 Creem webhook body 的实扣字段如 object.order.amount),或以 `pricing.payable_cents` 回填订单实付;退款/补差价基数一律读实扣额。

---

## 四、P1 — 业务缺陷 / 安全

### P1-1 实时 ASR 只在 finished 一次性扣费,断连/余额不足即白嫖【资损·代码确认】
- 位置:`backend/app/services/audio/realtime_asr.py:114`(仅 ensure_available 查>0)、`:311-326`(仅 provider `finished` 事件才 `_charge_realtime_asr`)、`:447-461`(all-or-nothing);断连 `from_client` 收 disconnect 只 `done.set()`(:165-167)→ task cancel → finished 永不到达 → 分文不扣,但用户已通过 partial/completed 拿到全部转写文本。
- 影响:保持余额≥1 即可无限用实时 ASR(唯一红区成本节点,~100 积分/分钟真金白银);录完主动断开稳定复现。
- 修复:会话内 30-60s 增量结算或按 max_duration 预扣多退少补;断连路径按 bytes_received 结算;charge 失败改"扣到 0"部分扣款。

### P1-2 支付发货多集合非原子串行写 + 无对账/清道夫,崩溃即掉单无告警
- 位置:`payment/app/api/v1/payments.py:517-536`(多集合串行写)、`webhook_ingest.py:92`(reclaim 只认 failed);硬崩溃后事件卡 processing/received,用户已付款却永久无权益。
- 约束:MongoDB standalone 不能用事务。修复:①processing 事件加超时回收(worker 扫描 stuck>N 分钟重放);②补 reconcile 对账 job 定期比对渠道账单 vs payment_orders/refunds;③关键节点失败告警。

### P1-3 Creem 订阅状态事件按 email 无差别覆盖,截断当前权益、破坏续费映射
- 位置:`payment/app/api/v1/payments.py:2366-2391 _handle_creem_subscription_status`,update filter 仅 `{email}`,无条件回写 latest_provider_subscription_id,canceled/expired 时改 subscription_expires_at/auto_renew,**不校验 subscription_id 是否当前订阅**。
- 触发:升级(月付→年付)创建新订阅 + 对旧订阅 scheduled 取消 → 旧订阅的 canceled 事件命中同 email,把到期倒回旧月付、auto_renew 置 YOUR_RecognizesAccessKey、latest id 倒回旧订阅。换渠道同理。
- 修复:更新用户级字段前先判 `subscription_id == user.latest_provider_subscription_id`,否则只更新订阅台账不回写 users。

### P1-4 升级闸门不感知渠道,Apple 订阅与 web 订阅可对同一用户并存双扣款
- 位置:`backend/app/api/v1/payments.py:946-969`(升级闸门只比 rank/账期)+ `payment/app/api/v1/apple_iap.py:255-273`;web 升级后 Apple 低档续费回调撞 `_resolve_change_mode` 抛 400,用户被真实扣款却丢权益。
- 修复:升级闸门纳入渠道判定,拒绝跨渠道并存订阅;Apple provider 放宽 change_mode 校验或走独立发货路径(见 P1-8)。

### P1-5 支付端内部 admin/退款/回调端点经网关公网可达【实证坐实】
- 位置:`docs/operations/gateway-nginx-preview.md:117` / prod.md:91 整前缀转发 8890;公网实测 admin/config、admin/orders/{id}/refund 返回 422(穿透到应用层),仅靠单一 HMAC 密钥、无网络层隔离。
- 修复:nginx 对 payment 的 /admin/ /callback/ 内部路径 return 403(参照号池 pool),或应用层加内网 CIDR 限制。

### P1-6 内部端口公网暴露(MongoDB 27017 + 8000/8888/8889/8890)【实证·运维】
- 位置:业务服务器全部 bind 0.0.0.0 且公网可连;MongoDB auth 已开但端口不应暴露。
- 修复:业务端口只对网关内网 IP 放行(安全组白名单 / bind 127.0.0.1+私网);属服务器加固,不在代码修复清单但必须告警运维。

### P1-7 pending_manual_refund(欠用户人工补退)对管理端不可见,漏退资损
- 位置:`payment/app/api/v1/payments.py:628-671`(建券失败回退全价时写 subscription_refund_events status=pending_manual_refund + logger.warning),但 admin 退款列表 `admin_refunds`(:1076)只查 payment_refunds,从不查 subscription_refund_events,无端点暴露。
- 修复:新增 admin 端点列出 pending_manual_refund,或这类记录同时写入 payment_refunds 进入既有列表。

### P1-8 Apple 消费型加购在"套餐积分未用完"时拒绝发货,但苹果已扣款
- 位置:`payment/app/api/v1/payments.py:2116`(`if _plan_remaining(user)>0: raise 400`)+ `apple_iap.py:461-466`(记 rejected 返回 200 不重试)。App Store 已扣款却无权益,违反 Apple IAP 合规红线。
- 修复:Apple 加购余额未清空不硬拒,无条件入账(paid_topup 叠加)或触发退款。

### P1-9 回调实付额与订单应付额无对账校验(欠额/篡额照常发货)
- 位置:`payment/app/api/v1/payments.py:1949`(仅校验 paid_amount>=0);权益发放不依赖金额,ZPay/Creem 均不比对 payable_cents。
- 修复:履约前比较实付≥应付(容差),不足落 mismatch 事件 + 告警 + 拒绝/人工复核。

### P1-10 users/credit_grants 三写者并发覆盖 + plan_credits_used 可为负【竞态·资损】
- 位置:`backend/app/repositories/user_repository.py:331-340 increment_plan_credits_used`(纯 $inc 无 guard);扣费 `credit_account_service.py:74-99` 持 `credits:{email}` 锁,但 payment 结算(payment payments.py:2031+)与周期重置 `reset_plan_period` 不持该锁。charge 锁内读 balance→$inc used,并发重置置 used=0 后旧请求 $inc 落地 → used 为负(变相多送)或新周期被预吃。写者≥4(backend PlanService/plan_admin 手动分配/auto-renew 开关/payment 结算退款)。
- 约束:standalone 无事务。修复:refund/charge 的 $inc 加条件(`used:{$gte:amount}`/`{$lte total}`)或以 plan_current_period_start 版本戳做条件更新;中期推进 rearchitecture 阶段2 projector 唯一写者。

### P1-11 payment 端 plan 清洗白名单缺 reset_period="none",M1 trial 语义在支付路径失效
- 位置:`payment/app/api/v1/payments.py:372 _clean_plan_config` reset_period 白名单 `{week,month,year}` 不含 backend M1(`8c6a254`)新增的 `none` → reset=none 套餐经 payment 按 month 重置(payments.py:2027 算 period_end)。当前仅 trial 用 none 且不可购未爆雷,但为设计文档警告的"两服务重复实现易漂移"实锤。
- 修复:payment 白名单补 none;根治见 P2 双实现收敛。

### P1-12 管理端 `POST /api/v1/users/credits` 局部 import 已删函数,运行时必 500
- 位置:`admin/backend/app/api/v1/users.py:139-140` 函数体内 `from app.api.v1.plans import _ensure_subscription_current`,该符号已在 f7e28af 随 plans.py 纯代理化删除。UsersView/Jinja users.html 查积分即 ImportError→500。
- 修复:改为经 plans 代理调后端 `GET /api/v1/config/plan-admin/subscription`(已含惰性推进),import 提顶。

---

## 五、P2 — 代码质量 / 架构隐患

- **P2-1** backend `subscription_purchase_service.py` 整套结算逻辑(apply_paid_purchase/apply_refund/schedule_paid_downgrade,51-509)app 内**零调用方**(真实履约在 payment 仓独立重实现),且含 payment 298d2cc 已废弃的假退款台账旧拷贝 → 活的陷阱。修复:加显式弃用注释或删除。
- **P2-2** subscription_lifecycle.py **三仓副本**(backend 140行/admin 193行/payment 153行)Mongo 语义暂等价但结构已漂移(FREE_PLAN_CODE 来源分叉);plan 清洗、升级闸门同样两仓平行实现共享库双写,无契约测试。修复:抽契约测试或收敛单模块(rearchitecture 阶段3)。
- **P2-3** admin `POST/GET/DELETE /api/v1/config/system`(config.py:123-156)裸键值端点仍存活,可写任意 key(含 YOUR_PLAN_CONFIGS_KEY/YOUR_BILLING_CONFIG_KEY)、GET 返回全部含内部密钥明文;前端无调用方,是绕过单一入口的僵尸后门。修复:删除三端点(保留 cleanup-obsolete)。
- **P2-4** admin Jinja 旧页只关一半(pages.py:90-92 仅下线 /config /plans),payment_providers.html/payment_orders.html/users.html 仍在线可写生产配置,sidebar 仍链向。修复:可写页 301 到 Vue 路由或整体下线。号池端同类 Jinja+SPA 双轨(pages.py+templates/)也未清理。
- **P2-5** v1/v2 audio_pipeline.py 逐字节复制(仅 logger 名不同),计费改动必双改(b6e9cf6 果然各改55行);text_pipeline 只有 v2 不对称。修复:抽公共基类。
- **P2-6** 账本 idempotency_key=uuid4(v1/v2 audio_pipeline.py:302-311),每次新随机键幂等永不生效但注释宣称防重复;text/realtime/quick_action 三处完全没传 idempotency_key。修复:改用请求级稳定 ID 四处统一。
- **P2-7** 后段失败全额退款含已消耗 ASR 成本(v2/audio_pipeline.py:319-325):余额够 ASR 不够 LLM 的用户每次白嫖 ASR 成本。修复:失败退款只退未消费节点。
- **P2-8** audio_cost 规则禁用/缺失仍最低扣 1(credit_calculator.py:112 max(1,...)),与 token_cost(credits=0→0)语义相反,管理端 enabled=false 停收费对 ASR 无效。修复:统一 credits<=0→0。
- **P2-9** 号池上游 api_key、proxy_config.password 明文入库(api_key_repository.py),GET /keys 列表/详情明文回显浏览器(打码仅前端 CSS);上游 key 与后端共库无加密。修复:静态加密(envelope)+ 列表脱敏+详情点击显示+审计。
- **P2-10** 号池消费记账与后端积分/usage 两套口径,LLM 链路 fire-and-forget 上报(llm_service.py:307 await 不一致)易静默丢失致漂移;上报无幂等键(pool.py:77)。修复:统一口径+幂等键。
- **P2-11** provider 配置缓存失效静默 best-effort(admin provider_config.py:300 吞异常+不看响应码)+进程内单点,通知失败最长 600s 计费/路由不一致,依赖 workers=1 隐式前提。修复:至少 log warning;多 worker 前改广播。
- **P2-12** payment_transactions 死集合(main.py:32 建索引+admin_order_detail:1045 读,全仓零写入),订单详情 transactions 永远空。修复:落库或移除。
- **P2-13** grant-credits 无金额上限无幂等(admin users.py:203-264,可发 21 亿、双击重复);字段 admin_email 实为用户名误导审计。修复:上限+防重+改名。留痕(admin_operation_log+credit_ledger 只增)已做对。
- **P2-14** 密钥/签名比较常量时间不一致:payment verify_callback_key `!=`(payments.py:201)、zpay.py:128 `.lower()!=`、admin alerts.py:21 `!=`,其余用 compare_digest。修复:统一 hmac.compare_digest。
- **P2-15** get_plan_config 未知/下架套餐静默回退 free(plan_repository.py:302-304),污染升级闸门(付费用户被当 free 放行重复购买/降级)与展示,无告警。修复:回退加告警日志或显式拒绝。
- **P2-16** processing 事件卡死后同 event_id 重试恒返回 duplicate 且快照空(subscription_purchase_service.py:511-526),权益永不发放无超时回收(与 P1-2 同源)。

## 六、P3 — 建议(择机)

弱默认 fail-fast(backend jwt_secret_key/admin SECRET_KEY/api-pool SECRET_KEY/各 INTERNAL_API_KEY/ALERT_INTERNAL_KEY 默认值启动即拒);内部密钥回退链去除(backend admin_config_internal_key、admin PROVIDER_CONFIG_INTERNAL_KEY 漏配应 fail 而非回退全局 api_key);内部链路只传 signature+key-id 不传明文密钥;300s 签名容差补 nonce 防重放;trial 到期上海日粒度 vs expires_at 时刻不一致(plan_service.py:523-534);change_subscription(downgrade_later)对付费目标不设 pending_requires_payment 的休眠 0 元激活陷阱(plan_service.py:229-237);计费规则 _clean_YOUR_CREDIT_PRICING_RULES_KEY 末尾无条件回填 DEFAULT 致运营删不掉(plan_repository.py:461-469);init_admin 初始密码明文进日志(三仓);登录限流 request.client.host 网关后失真不读 XFF(admin/api-pool);改密不失效存量会话;死导入(payments.py:20 settings、plan_admin.py:29 TRIAL_PLAN_CODE);$regex 直拼用户输入(ledger.py:22、users.py:78、plan_admin.py:205);地理探测走明文 http://ip-api.com;credit-billing-management-design.md 文档已被 billing-plan-refactor-design.md 取代仍自称权威契约;部署文档三个废弃 env 键(PAYMENT_SERVICE_URL 等已迁 DB);YOUR_PAYMENT_RUNTIME_CONFIG_KEY 无写入口/管理页;孤儿端点 /plans/subscription/auto-renew 无 UI 调用;登录响应体多返回明文 JWT;TRUSTED_PROXY_CIDRS 默认信任全 RFC1918 未收敛;号池 token/seconds 额度 pick 不预占高并发可击穿软额度;trial_once_per_user 定义齐全但三仓无 enforcement。

## 七、已确认"做对了"的关键项(回归时勿误改)

金额全程整数分;YOUR_PLAN_CONFIGS_KEY 单写者单入口(admin 纯 HMAC 代理,catalog publish 不再投影 YOUR_PLAN_CONFIGS_KEY,死投影已删);webhook 双层幂等(外层 provider+event_id、内层 payment_event_id,验签失败独立键空间);Creem/ZPay/Apple 验签完备;服务端权威定价+补差价按渠道分流;跨渠道防双扣(03080aa best_effort_cancel_stale);折扣券五重防复用;假退款台账已废除改诚实台账;cancel-renewal scheduled 语义正确;Apple 交易归属绑定防重放;后端 subscription_status 非 active 值由 payment 写(跨服务契约,勿当死代码清);号池 pick 用 find_one_and_update 原子领用;号池内部 HMAC 鉴权最规范;各端 bcrypt+登录限流+HttpOnly/Secure Cookie。

---

## 八、修复计划(按优先级与依赖分批)

### 批次 1:P0 资损止血(最高优先,本次必做)
1. P0-1 零元续费:删除 apply_due_auto_charge 本地权益发放 + 客户端 auto-renew 端点补渠道校验 + 冲正存量零元事件。
2. P0-2 Creem 升级记账:履约/退款/补差价基数改读实扣额。
3. P1-1 实时 ASR 断连白嫖:断连路径按已收字节结算 + charge 失败部分扣款。
4. P2-7 后段失败退款保留已消耗 ASR 成本、P2-8 audio_cost 停收费生效(与 P0 同属扣费真实性,一并修)。

### 批次 2:P1 安全与业务闭环
5. P1-5 nginx 隔离 payment 内部端点(参照号池)+ P1-6 端口安全组(运维配合)。
6. P1-12 admin users.py 断链 500(改代理+import 提顶)。
7. P1-3 Creem 状态事件加当前订阅判定;P1-11 payment 清洗白名单补 none。
8. P1-2/P2-16 processing 超时回收 + reconcile 对账 job + P1-9 金额对账闸门 + P1-7 pending_manual_refund 可见;P1-8 Apple 加购不硬拒。
9. P1-10 users 计费字段 $inc 加条件 guard(standalone 无事务替代方案)。
10. P1-4 升级闸门渠道感知(需与 P1-3/P1-8 一并设计)。

### 批次 3:P2 清理与防漂移
11. P2-1 删/标注 backend 僵尸结算镜像;P2-3 删 /config/system 后门;P2-4 下线可写 Jinja 旧页;payment backend/ 僵尸目录删除;P2-12 死集合处理。
12. P2-6 账本幂等键统一稳定 ID;P2-5 audio_pipeline 抽基类;P2-2 subscription_lifecycle 契约测试;P2-14 常量时间比较统一。
13. P2-9 号池密钥加密+脱敏;P2-13 grant 上限+幂等;P2-15 未知套餐回退告警。

### 批次 4:P3 加固(择机,部分随批次2/3捎带)
弱默认 fail-fast、密钥回退链去除、限流读 XFF、文档漂移修正、死导入清理等。

> 注:批次1/2 的代码修复本次执行并部署 preview 验证;涉及运维(端口安全组、生产 .env)与外部渠道真实数据(P0-2 生产监控)的部分需协同,文档中标注交接。

---

## 九、修复执行记录(2026-07-13 本次)

### 已修复并部署 preview 验证(三仓已提交推送)

提交:backend `bd28506`(main)、admin `1539a3f`(main)、payment `abdb345`(master)。
验证:backend 209 项单测全过、payment 68 项单测全过、三服务健康启动无 import 错误、P0-1 以 mock 到期 zpay+auto_renew 用户实测(改后走 free 兜底、零元续费事件增量为 0,mock 数据已清理)。

| 编号 | 问题 | 修复 |
|---|---|---|
| P0-1 | 零元自动续费白嫖(已实证发生) | `_try_due_auto_charge` 停用本地零元续期、续费只认渠道回调;`apply_due_auto_charge` 加 deprecated;客户端 auto-renew 端点补渠道校验(拒 zpay、Creem 取消后重开) |
| P0-2 | Creem 折扣升级全价记账双向资损 | 履约改用 `payment_orders.amount_cents`(服务端权威实付/差价)记账,回退全价保护普通购买 |
| P1-1 | 实时 ASR 断连/余额不足白嫖 | 收尾兜底按已收音频时长补扣;`_charge_realtime_asr` 改扣到可用余额(部分扣款) |
| P1-3 | Creem 状态事件覆盖当前订阅 | 加"当前订阅"判定,旧订阅事件不再改写 users;latest_provider_subscription_id 只由履约维护 |
| P1-7 | pending_manual_refund 漏退 | 同时 upsert 到 payment_refunds,管理端退款列表可见 |
| P1-11 | payment 清洗漏 reset_period=none | 白名单加 none + `_add_period` 支持 none(对齐 backend M1) |
| P1-12 | admin 查积分接口必 500 | 删局部 import 已删函数,订阅推进收敛到后端 |
| P2-3 | admin /config/system 裸键值后门 | 删除 GET/POST/DELETE 三端点 |
| P2-8 | audio_cost 停收费不生效 | credits<=0 时不再最低扣 1,对齐 token_cost |
| P2-14 | 三处非常量时间比较 | payment verify_callback_key / zpay 验签 / admin alerts 改 hmac.compare_digest |

### 需后续迭代(本次未改,附原因)

**架构级(高风险,不宜在审计中贸然重构,建议独立迭代 + 充分测试):**
- P1-2 / P2-16 支付发货多集合非原子写、无 reconcile 对账 job、processing 卡死无超时回收 → 需设计对账/清道夫机制(MongoDB standalone 无事务,不能用事务方案)。
- P1-4 升级闸门渠道感知(Apple/web 双扣款)→ 需与 P1-3/P1-8 一并重设计跨渠道订阅约束。
- P1-9 回调金额对账闸门 → 需定义容差与 mismatch 处置流程。
- P1-10 users/credit_grants 多写者竞态($inc 无 guard)→ 落在高频核心扣费路径,需跨仓共享锁或推进 rearchitecture 阶段2 projector,不宜贸然改。
- P2-1/P2-2 backend 僵尸结算镜像、subscription_lifecycle 三仓副本 → 需收敛单实现 + 契约测试(rearchitecture 阶段3)。
- P2-5 v1/v2 audio_pipeline 复制、P2-6 账本幂等键 uuid4 → 需抽公共基类 + 请求级稳定 ID,改动面广需回归。
- P2-9 号池密钥明文存储/回显 → 需引入 envelope 加密层。

**运维级(非代码,需服务器/网关操作):**
- P1-5 支付端内部 admin 端点经网关公网可达 → nginx 对 /admin/ /callback/ 内部路径 return 403(参照号池)。
- P1-6 MongoDB 27017 + 业务端口公网暴露 → 安全组白名单 / bind 内网。
- 生产 .env 弱默认密钥核查、backend→admin 内部密钥去回退链。

**需外部真实数据验证(preview 无法端到端):**
- P0-2 修复方向正确(用自存的实付额),但 Creem 真实折扣升级 webhook 无法在 preview 伪造签名 → 上线后需抓一次真实升级 webhook 对账 product.price vs amount_cents,确认实扣字段。

**P1-8 Apple 加购余额未清空拒发货**:改动涉及 Apple IAP 合规发货/退款流程,需 sandbox 验证,建议随 Apple 相关迭代一并处理。

**P2-4 Jinja 半下线、P2-7 后段失败退款保留 ASR 成本、P2-12/13/15 及全部 P3**:低危,建议随日常迭代捎带。

### 服务器加固备忘(交接运维)
1. MongoDB/业务端口(27017/8000/8888/8889/8890)当前 bind 0.0.0.0 且公网可连,生产必须收敛。
2. 支付端内部端点经网关公网返回 422(穿透到应用层),需 nginx 层隔离。
3. 各服务弱默认密钥 preview 已 .env 覆盖,但代码层建议加"默认值即 fail-fast"。
