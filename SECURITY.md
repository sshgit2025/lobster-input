# 安全策略

请勿在公开 Issue、PR、讨论或日志中提交可利用细节、真实访问令牌、数据库内容或个人信息。

GitHub 仓库启用私密漏洞报告后，请使用仓库 Security 页的 “Report a vulnerability” 报告。若入口不可用，可提交只包含“请求私密安全联系方式”的 Issue，等待维护者提供私密渠道后再传递细节。

报告应说明受影响版本、复现条件、影响范围和不含真实凭据的最小示例。目前安全修复优先面向默认分支，不承诺响应时限或历史版本维护。

部署者必须自行生成 JWT、内部服务、数据库和签名密钥；不能直接使用 `.env.example` 中的占位值。发现密钥暴露后应立即撤销或轮换，删除文件或重写历史不能替代轮换。

数据库备份、账号数据、录音和真实配置必须保存在仓库外。开源发布前运行仓库检查与完整密钥扫描，并在 GitHub 开启 Secret scanning 和 Push protection（可用时）。
